# Python
__pycache__/
*.py[cod]
*.egg-info/
.env

# Jupyter checkpoints
.ipynb_checkpoints/

# Large model cache (can be hundreds of MB)
data/models_cache.pkl

# Claude working files
.claude/
_fix_nb.py
upgrade_notebook.py

# Old notebook versions (v22 -> v23.4) are now TRACKED, not ignored — they were
# local-only with no git safety net. Archival lineage only; the live model is
# trading_model_v25.1.ipynb. Verified secret-free before committing (Alpaca refs
# are "APCA-API-KEY-ID": api_key header names, never literal keys).

# Dashboard data feed — pushed to orphan `data` branch each cycle, never accumulated in master
docs/data.json

# local DPAPI-encrypted gh token for scheduled triggers (never commit)
scripts/.gh_token.dpapi

# local trigger/watch logs from the workstation scheduled tasks
run_logs/
